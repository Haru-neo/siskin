# Reads memory that has already been freed. The debug build catches it right there.
fn main():
    unsafe:
        let p = alloc[Int](4)
        p[0] = 42
        free(p)
        print(str(p[0]) + "\n")
