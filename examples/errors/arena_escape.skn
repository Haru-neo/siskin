# Tries to send a value created inside an arena out of the block.
# That memory disappears when the block ends, so the compiler stops it.
fn build() -> [Int]:
    with arena a:
        var xs = a.list[Int]()
        xs.push(1)
        return xs
    return []

fn main():
    print(str(build()) + "\n")
