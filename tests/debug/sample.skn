struct Box:
    items: [Int]

    fn total(self) -> Int:
        var t = 0
        for x in self.items:
            t += x
        return t

fn double(n: Int) -> Int:
    let r = n * 2
    return r

fn main():
    let b = Box(items: [1, 2, 3])
    var s = b.total()
    s = double(s)
    print(f"twice the sum: {s}\n")
    let name = "Mika"
    print(name + "\n")
